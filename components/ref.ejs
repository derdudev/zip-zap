<% console.log({href: "#"+href, children: [value] }); %>
<% console.log(include("in-out-link", {href: "#"+href, children: [value] })) %>
<%- include("in-out-link", {href: "#"+href, children: [value] }); %>

